@file:Suppress("NOTHING_TO_INLINE")

package info.spiralframework.base.binding

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Any?) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Int) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Long) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Byte) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Short) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Char) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Boolean) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Float) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: Double) {
    System.err.print(error)
}

/** Prints the given [error] to the standard error stream. */
public actual inline fun printErr(error: CharArray) {
    System.err.print(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Any?) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Int) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Long) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Byte) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Short) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Char) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Boolean) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Float) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: Double) {
    System.err.println(error)
}

/** Prints the given [error] and the line separator to the standard error stream. */
public actual inline fun printlnErr(error: CharArray) {
    System.err.println(error)
}